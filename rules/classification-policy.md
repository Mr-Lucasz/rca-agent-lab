# Classificação

Executar aliases e regras antes de sugestões semânticas. Preservar valores reportados e preencher apenas `agent_suggested_*`. Marcar `consistent`, `questionable`, `unsupported`, `conflicting` ou `insufficient_evidence`.

