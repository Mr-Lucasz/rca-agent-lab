{
  "name": "rca-agent-lab",
  "version": "1.0.0",
  "private": true,
  "description": "Agente orquestrador para RCA auditável de bases de bugs",
  "scripts": {
    "rca": "python scripts/rca.py",
    "demo": "python scripts/rca.py analyze data/input/bugs-demo.csv --output reports/demo --mode rules",
    "lint": "python scripts/check_repo.py",
    "typecheck": "python -m compileall -q src scripts tests",
    "test": "python -m pytest -q",
    "check": "npm run lint && npm run typecheck && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}

