---
name: build-rca-html
description: Gerar dashboard HTML RCA autocontido a partir do objeto canônico. Usar depois da revisão semântica e das ações, sem criar números fora do modelo de dados.
---

# Gerar dashboard

Executar `finalize` com o review validado. Não editar HTML manualmente. Garantir que resumo, KPIs, clusters, hipóteses, ações e drilldown derivem do mesmo objeto. Manter HTML sem servidor, CDN ou chamadas externas. Exibir qualidade, fontes, limitações e `requires_human_review`.

