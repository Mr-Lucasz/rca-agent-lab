fields:
  bug_id: [id, bug_id, bugid, key, chave, ticket, issue, codigo]
  title: [title, titulo, resumo, summary, bug]
  severity: [severity, severidade, priority, prioridade]
  description: [description, descricao, detalhes, detail]
  expected_behavior: [expected_behavior, comportamento_esperado, expected, resultado_esperado]
  actual_behavior: [actual_behavior, comportamento_obtido, comportamento_atual, actual, resultado_obtido]
  preconditions: [preconditions, pre_condicoes, precondicoes, setup]
  bug_type: [bug_type, tipo_bug, tipo_de_bug, defect_type]
  root_cause_category: [root_cause_category, causa_raiz, categoria_causa_raiz, root_cause]
  root_cause_cluster: [root_cause_cluster, cluster_causa_raiz, rca_cluster]
  affected_module: [affected_module, modulo_afetado, modulo, module, component]
  environment: [environment, ambiente, env, detection_environment]
  status: [status, situacao, state]
  created_at: [created_at, data_criacao, created, opened_at]
  detected_at: [detected_at, data_deteccao, detection_date]
  resolved_at: [resolved_at, data_resolucao, closed_at, fixed_at]
  reopened: [reopened, reaberto, reabertura]
  dev_analysis_notes: [dev_analysis_notes, notas_dev, dev_notes, analise_dev]
  qa_analysis_notes: [qa_analysis_notes, notas_qa, qa_notes, analise_qa]
  version: [version, versao, release, build]
  team: [team, time, squad]
  prd_reference: [prd_reference, prd, requisito, requirement]
values:
  unknown: [unknown, desconhecido, nao_informado, não informado, n/a, na, "-", ""]

